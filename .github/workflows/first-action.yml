name: First Workflow
on: workflow_dispatch
jobs:
 first-job:
  runs-on: ubuntu-latest
  steps:
   - name: print greeting
     run: echo "hello-world welcome to github workflow"
   - name: display files
     run: ls -ltr
   - name: print bye
     run: echo "bye from sandesh"
   - name: create file
     run: touch nash.txt
